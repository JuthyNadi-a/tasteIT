import React  from 'react';
import Form from './Form';

const Add = () => {
    return (
        <div>
            <p>Add more recipes</p>
            <Form/>
        </div>
    );
};

export default Add;